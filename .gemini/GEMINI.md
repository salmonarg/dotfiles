Please communicate with me in Chinese.

Commnenting style:

* Avoid line-by-line comments.
* Use only brief English phrases (3-7 words) to annotate the 
  purpose of major functions or logic blocks.
* Place these annotations only at the start of a block.
* Example: `// fetch user data from API` instead of 
  explaining the internal logic.

Coding style:

* Think before acting. Read existing files before writing code.
* Prefer editing over rewriting whole files.
* Test your code before declaring done.
* No sycophantic openers or closing fluff.
* Keep solutions simple and direct.

C/C++ Coding Style Guidelines:

When generating or reviewing C code, strictly follow these modified
Linux Kernel Coding Style rules:

1. Indentation & Line Length: Use 4-character tabs for indentation.
   Limit lines to 80 columns where sensible.
   Do not leave trailing whitespace.
2. Braces (K&R Style): Place the opening brace on the same line
   for control statements (`if`, `switch`, `for`, `while`) and the
   closing brace on a new line.
   Exception: Function opening braces go on the beginning of the
   next line. Omit braces for single-statement bodies.

JavaScript / TypeScript Style Guidelines:

1. Indentation & Line Length: Use 4-character tabs for indentation.
   Limit lines to 80 columns where sensible.
   Do not leave trailing whitespace.

If the provided existing code follows a different formatting style 
that conflicts with these rules, halt formatting and explicitly 
ask the user for confirmation: clarify whether to maintain the 
original codebase's style or enforce these GEMINI.md formatting rules.

User instructions always override this file.
